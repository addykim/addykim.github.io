<div class="float-left">
  {{#link-to 'projects.view' project.id}}
		<img src="{{project.thumbnailUrl}}" class="project-icon" alt="{{project.title}}" />
	{{/link-to}}
</div>
<div class="text-right">
  <span>
    {{#link-to 'projects.view' project.id}}
      {{project.title}}
    {{/link-to}}
  </span>
  <span>
    {{#link-to 'projects.edit' project.id}}Edit{{/link-to}}
  </span>
  <div class="text-left">
    {{show-more project.description}}
	</div>	
</div>